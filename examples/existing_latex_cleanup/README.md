# Existing LaTeX Cleanup

This example shows how to move from a messy LaTeX/Overleaf export to a clean manuscript repository.

Compare:

- `before_structure.txt`
- `after_structure.txt`

Then read [../../docs/existing_latex_project.md](../../docs/existing_latex_project.md).
